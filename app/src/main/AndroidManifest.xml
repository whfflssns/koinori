<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.koinori">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Koinori">
        <activity android:name=".menu.MenuActivity"></activity>
        <activity android:name=".menu.Menu_coffeeActivity"/>
        <activity android:name=".menu.Menu_dessertActivity"/>
        <activity android:name=".menu.Menu_smoo_adeActivity"/>
        <activity android:name=".menu.Menu_teaActivity"/>
        <activity android:name=".reserve_room.reserve_room1" />
        <activity android:name=".ReserveActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserdataActivity" />
        <activity android:name=".UserCreateActivity" />
    </application>

</manifest>